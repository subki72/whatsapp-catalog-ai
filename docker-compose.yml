version: '3.8'
services:
  wa-catalog-backend:
    build: .
    container_name: wa-catalog-ai
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped